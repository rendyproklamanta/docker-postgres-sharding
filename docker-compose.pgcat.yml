version: "3"

services:
  pgcat:
    image: ghcr.io/postgresml/pgcat:latest
    container_name: pgcat
    volumes:
      - ./pgcat.toml:/etc/pgcat/pgcat.toml
    ports:
      - "6432:6432"
      - "9930:9930"
    networks:
      - yugabyte-network

networks:
  yugabyte-network:
    driver: bridge
